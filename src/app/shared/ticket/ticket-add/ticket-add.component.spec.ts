import { TicketAddComponent } from "./ticket-add.component";

describe("TicketAddComponent", () => {
  let component: TicketAddComponent;

  beforeEach(() => {
    component = new TicketAddComponent();
  });

  it("should create", () => {
    expect(component).toBeTruthy();
  });
});
